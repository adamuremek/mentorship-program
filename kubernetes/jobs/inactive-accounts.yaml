apiVersion: batch/v1
kind: CronJob
metadata:
  name: deactivate-inactive-accounts
spec:
  schedule: "* 0 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: web
            image: localhost:5000/project-kubernetes
            command:
            - python manage.py runscript /mentorship_program/mentorship_program_project/mentorship_program_app/scripts/deactivate_inactive_accounts.py
          restartPolicy: OnFailure
